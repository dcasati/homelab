apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wikipedia-offline

resources:
  - ns.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - cronjob-refresh.yaml  # optional
  - configmap.zim-urls.yaml
